---
'manifest': minor
---

Make harness model catalogs opt-in, represent reasoning levels as selectors on a single model, support optional fixed-effort aliases, and remap Chat Completions or Responses reasoning requests to each attempted provider.
